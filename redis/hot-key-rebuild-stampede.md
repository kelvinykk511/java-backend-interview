# 熱點 Key 重建（擊穿／Stampede）

## 面試問題

熱點緩存剛好過期，瞬間幾千請求同時 miss，怎麼避免一起打垮 DB？（和穿透、雪崩怎麼分）

## 答法

**先對齊名詞**

- **擊穿（stampede）**：一個**熱點** key 過期 → 併發重建。
- **穿透**：key **根本不存在** → 每次都打 DB。
- **雪崩**：大量 key **同時**過期，或 Redis 掛了。

**三種重建策略（面試主線）**

1. **互斥鎖 / Singleflight**：只有拿到鎖的請求查 DB 並回寫；其他人短暫等待或讀舊值。代價：鎖超時、單點重建延遲。
2. **邏輯過期**：value 裡帶 `expireAt`，物理上永不過期；過期後仍返回舊值，並**異步**觸發一次重建。代價：短暫讀到舊數據（可接受則很香）。
3. **熱點永不（或極長）過期 + 主動更新**：寫路徑刪/更新緩存；讀路徑幾乎不 miss。代價：寫複雜、要防漏更新。

面試口訣：**熱點少用「純 TTL 到期再全員重建」；用鎖串行化，或邏輯過期換一致性窗口。**

**實作細節（加分）**

- 鎖用 Redis `SET key token NX PX`，釋放要核對 token（防誤刪別人的鎖）。
- 等待方要設超時，避免雪崩式排隊把線程池打滿（Spring MVC 尤其痛）。
- 空結果仍要短 TTL 緩存，否則擊穿修復了、穿透還在。

## 常見追問 / 陷阱

「加分布式鎖重建，是不是就 Exactly-Once 寫入緩存了？」

→ 鎖只保證「同時只有一個去查 DB」。DB 查完、寫 Redis 前進程掛了，別人還會再查一次——**可接受的重複查庫**，不是業務冪等問題。別把緩存重建鎖和「扣款冪等」混談。

## 小練習

秒殺商品詳情頁，QPS 極高、可接受 1～2 秒舊庫存展示：互斥鎖和邏輯過期你選哪個？為什麼？
